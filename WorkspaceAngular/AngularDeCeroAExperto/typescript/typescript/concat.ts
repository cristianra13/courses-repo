(function()
{
    
    function getEdad(){
        return 100+100+100;
    }

    const nombre = 'Cristian';
    const apellido = 'Real';
    const edad = 27;

    const salida = `${nombre} ${apellido} (${getEdad()})`;
    
    console.log(salida);
    
})();
