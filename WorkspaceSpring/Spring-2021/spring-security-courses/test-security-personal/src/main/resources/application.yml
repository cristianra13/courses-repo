

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/support_portal?serverTimezone=UTC
    password: user_dev_pass
    username: user_dev
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

jwt:
  secret-key: my-secret-key