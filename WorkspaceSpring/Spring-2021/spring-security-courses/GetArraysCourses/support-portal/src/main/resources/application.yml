jwt:
  secret: '[a-zA-Z0-9._]^+$Guidelines89797987forAlphabeticalArrayNumeralsandOtherSymbo$'

server:
  port: 8081
  # Habilita el error 404 en una linea subrayada - es más estilo visual (Tipo tomcat)
  #error:
  #  whitelabel:
  #    enabled: false

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/support_portal
    username: root
    password: Cr1st1anr34l..
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

# URLs permitidas para consumo
#urls:
#  - http://localhost:4200
#  - http://localhost:3000

# Permite controlar el error 404 desde ExceptionHandling -> clase propia
  #mvc:
  #  throw-exception-if-no-handler-found: true
  #web:
  #  resources:
  #    add-mappings: false